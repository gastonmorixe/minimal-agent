{
  "name": "minimal-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Umbrella monorepo: git submodules for minimal-agent-core + minimal-agent-plugins.",
  "homepage": "https://github.com/gastonmorixe/minimal-agent",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/gastonmorixe/minimal-agent.git"
  },
  "type": "module",
  "scripts": {
    "install:all": "bun install && bun install --cwd minimal-agent-core && bun install --cwd minimal-agent-plugins",
    "start": "bun run --cwd minimal-agent-core start",
    "test": "bun run --cwd minimal-agent-core test && bun run --cwd minimal-agent-plugins test",
    "test:plugins": "bun run --cwd minimal-agent-plugins test:plugins",
    "check": "bun run --cwd minimal-agent-core check && bun run --cwd minimal-agent-plugins check",
    "typecheck": "bun run --cwd minimal-agent-core typecheck && bun run --cwd minimal-agent-plugins typecheck",
    "link:plugins": "bun run scripts/link-plugins.ts",
    "submodules:status": "git submodule status",
    "submodules:update": "git submodule update --remote --merge",
    "submodules:init": "git submodule update --init --recursive",
    "version": "bun run scripts/version.ts",
    "version:status": "bun run scripts/version.ts status",
    "commitlint": "commitlint",
    "commitlint:last": "commitlint --last --verbose",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0",
    "bun-types": "^1.3.14",
    "husky": "^9.1.7",
    "typescript": "^7.0.2"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "engines": {
    "bun": ">=1.3.14"
  }
}
